/**
 * DO NOT EDIT THIS FILE DIRECTLY
 * ----------------------------------
 * This file was generated by ISL Compiler.
 * Source: domain-ruleset.isl.md
 * Edit the ISL file instead.
 */

/**
 * @file This file defines the domain entities for the Heroquest game ruleset.
 * It provides factory functions for creating instances of Hero, Monster, Equipment, Item, and TreasureCard.
 * Role: Domain
 */

/**
 * Hero Factory Function
 * Creates a new Hero object with default values or values provided in the data object.
 * @param {object} [data={}] - Optional data object to initialize the Hero.
 * @returns {object} A Hero object.
 */
export const Hero = (data = {}) => {
  const {
    id = 0,
    classe = '',
    attacco = 0,
    difesa = 0,
    movimento = 0,
    mente = 0,
    corpo = 0,
    miniature = '',
    miniatureDeath = '',
    portrait = '',
  } = data;

  return {
    id,
    classe,
    attacco,
    difesa,
    movimento,
    mente,
    corpo,
    miniature,
    miniatureDeath,
    portrait,
  };
};

/**
 * Monster Factory Function
 * Creates a new Monster object with default values or values provided in the data object.
 * @param {object} [data={}] - Optional data object to initialize the Monster.
 * @returns {object} A Monster object.
 */
export const Monster = (data = {}) => {
  const {
    id = 0,
    nome = '',
    movimento = 0,
    attacco = 0,
    difesa = 0,
    corpo = 0,
    mente = 0,
    immagine = '',
    immalarge = '',
    nonmorto = false,
  } = data;

  return {
    id,
    nome,
    movimento,
    attacco,
    difesa,
    corpo,
    mente,
    immagine,
    immalarge,
    nonmorto,
  };
};

/**
 * Equipment Factory Function
 * Creates a new Equipment object with default values or values provided in the data object.
 * @param {object} [data={}] - Optional data object to initialize the Equipment.
 * @returns {object} An Equipment object.
 */
export const Equipment = (data = {}) => {
  const {
    id = 0,
    nome = '',
    dadatt = 0,
    daddif = 0,
    daddifex = 0,
    numdadicontr = 0,
    doppioatt = false,
    mosdoppio = 0,
    puntimente = 0,
    doppiamag = false,
    movim = 0,
    noogg = 0,
    diago = false,
    tiro = false,
    tirounavo = false,
    disinnesc = false,
    nopsg = false,
    nopsgid = 0,
    solopsg = false,
    solopsgid = 0,
    prezzo = 0,
    immagine = '',
  } = data;

  return {
    id,
    nome,
    dadatt,
    daddif,
    daddifex,
    numdadicontr,
    doppioatt,
    mosdoppio,
    puntimente,
    doppiamag,
    movim,
    noogg,
    diago,
    tiro,
    tirounavo,
    disinnesc,
    nopsg,
    nopsgid,
    solopsg,
    solopsgid,
    prezzo,
    immagine,
  };
};

/**
 * Item Factory Function
 * Creates a new Item object with default values or values provided in the data object.
 * @param {object} [data={}] - Optional data object to initialize the Item.
 * @returns {object} An Item object.
 */
export const Item = (data = {}) => {
  const {
    id = 0,
    nome = '',
    hp = 0,
    mp = 0,
    movimento = 0,
    attacco = 0,
    difesa = 0,
    natt = 0,
    acqua = false,
    danni = 0,
  } = data;

  return {
    id,
    nome,
    hp,
    mp,
    movimento,
    attacco,
    difesa,
    natt,
    acqua,
    danni,
  };
};

/**
 * TreasureCard Factory Function
 * Creates a new TreasureCard object with default values or values provided in the data object.
 * @param {object} [data={}] - Optional data object to initialize the TreasureCard.
 * @returns {object} A TreasureCard object.
 */
export const TreasureCard = (data = {}) => {
  const {
    id = 0,
    effetto = '',
    azione = '',
    valore = 0,
    immagine = '',
  } = data;

  return {
    id,
    effetto,
    azione,
    valore,
    immagine,
  };
};