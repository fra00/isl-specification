/**
 * 🛑 DO NOT EDIT THIS FILE DIRECTLY
 * ----------------------------------
 * This file was generated by ISL Compiler.
 * Source: domain.isl.md
 * Edit the ISL file instead.
 */

// domain.js

// Helper for generating UUIDs, assuming crypto.randomUUID is available in the environment.
// If not, a polyfill or alternative simple unique ID generator would be needed.
const generateUUID = () => crypto.randomUUID();

/**
 * An enumeration representing the possible visual states of a card.
 * @type {object}
 */
export const CardState = {
  Covered: 'Covered',
  Flipped: 'Flipped',
  Solved: 'Solved',
};

/**
 * Factory function for CardEntity.
 * Represents a single card on the game board.
 * @param {object} [data={}] - Initial data for the card.
 * @param {string} [data.id] - A unique identifier for the card instance.
 * @param {string} [data.value] - The identifier for the card's symbol or image.
 * @param {string} [data.state] - The current visual state of the card (from CardState).
 * @returns {object} A new CardEntity object.
 */
export const CardEntity = (data = {}) => {
  const {
    id = generateUUID(),
    value = '',
    state = CardState.Covered,
  } = data;

  return {
    id,
    value,
    state,
  };
};

/**
 * An enumeration representing the overall state of the game.
 * @type {object}
 */
export const GameStatus = {
  NotStarted: 'NotStarted',
  Playing: 'Playing',
  Paused: 'Paused',
  Won: 'Won',
};

/**
 * Factory function for GameConfig.
 * Configuration parameters for the Memory game.
 * @param {object} [data={}] - Initial configuration data.
 * @param {number[]} [data.gridSize] - An array [rows, columns] defining the dimensions of the card grid.
 * @param {number} [data.matchDelayMs] - The duration cards remain Flipped if they do not match.
 * @returns {object} A new GameConfig object.
 */
export const GameConfig = (data = {}) => {
  const {
    gridSize = [4, 4], // Default to a 4x4 grid
    matchDelayMs = 1000, // Default to 1000ms (1 second) delay
  } = data;

  return {
    gridSize,
    matchDelayMs,
  };
};