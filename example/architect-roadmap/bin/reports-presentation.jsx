/**
 * DO NOT EDIT THIS FILE DIRECTLY
 * ----------------------------------
 * This file was generated by ISL Compiler.
 * Source: reports-presentation.isl.md
 * Edit the ISL file instead.
 */

import React, { useState, useEffect, useCallback } from 'react';
import { ProjectStatus, EpicStatus, StoryStatus, ChangelogEntityType, ChangelogEntry } from "./domain";
import { StatusBadge } from "./ui-components";

/**
 * @typedef {object} ProgressTrendDataPoint
 * @property {string} date - ISO 8601 date string, e.g., "YYYY-MM-DD"
 * @property {number} progress - 0-100, the progress percentage at that date
 */

/**
 * @typedef {object} ProgressTrendChartSeries
 * @property {string} name - Name of the entity, e.g., Project Name
 * @property {ProgressTrendDataPoint[]} data
 */

/**
 * @typedef {object} EpicStatusSummary
 * @property {'NOT_STARTED' | 'IN_PROGRESS' | 'COMPLETED' | 'BLOCKED'} status
 * @property {number} count
 */

/**
 * @typedef {object} QuarterSummaryEntry
 * @property {string} quarter - e.g., "Q1"
 * @property {number} year
 * @property {EpicStatusSummary[]} epicSummaries - Breakdown of epics by status
 * @property {number} totalEpics - Total number of epics in this quarter
 */

/**
 * @typedef {object} ChangelogFilters
 * @property {'PROJECT' | 'EPIC' | 'STORY' | 'TEAM_MEMBER' | 'CATEGORY'} [entityType] - Optional, filters by the type of entity modified
 * @property {string} [entityId] - Optional, filters by the specific ID of the modified entity
 * @property {string} [field] - Optional, filters by the name of the field that was modified
 * @property {string} [startDate] - Optional, ISO 8601 date string, filters entries from this date onwards
 * @property {string} [endDate] - Optional, ISO 8601 date string, filters entries up to this date
 */

/**
 * @typedef {object} ReportsPresentationProps
 * @property {ProgressTrendChartSeries[]} seriesData - Data for the progress trend chart.
 * @property {QuarterSummaryEntry[]} summaryData - Data for the quarter summary.
 * @property {ReturnType<typeof ChangelogEntry>[]} changelogEntries - Data for the changelog table.
 * @property {ChangelogFilters} currentChangelogFilters - Current filter values for the changelog.
 * @property {(newFilters: ChangelogFilters) => void} onApplyChangelogFilters - Callback to apply new changelog filters.
 * @property {() => void} onClearChangelogFilters - Callback to clear changelog filters.
 */

/**
 * ReportsPresentation component displays various reports including progress trends, quarter summaries, and a changelog table.
 * @param {ReportsPresentationProps} props
 * @returns {React.Element}
 */
export default function ReportsPresentation({
  seriesData,
  summaryData,
  changelogEntries,
  currentChangelogFilters,
  onApplyChangelogFilters,
  onClearChangelogFilters,
}) {
  // RenderProgressTrendChart
  const RenderProgressTrendChart = useCallback(() => {
    // Since no specific chart component is provided in the dependencies,
    // we render a placeholder div and display the data that would be used by a chart component.
    // This adheres to the "NON fare mock" rule by not inventing a chart component,
    // while still showing the data structure that the capability expects to visualize.
    return (
      <section className="mb-8 p-4 bg-white shadow rounded-lg">
        <h2 className="text-xl font-semibold mb-4">Progress Trend Chart</h2>
        <div className="border border-gray-300 p-4 min-h-[200px] flex items-center justify-center text-gray-500">
          <p>
            [Chart Component Placeholder] - A line chart would be rendered here.
            <br />
            Data provided for charting:
          </p>
          <pre className="text-xs bg-gray-100 p-2 rounded overflow-auto max-h-40">
            {JSON.stringify(seriesData, null, 2)}
          </pre>
        </div>
        <p className="mt-2 text-sm text-gray-600">
          The chart would visualize progress over time (X-axis: date, Y-axis: progress 0-100),
          with clear labels, legend, and interactive tooltips.
        </p>
      </section>
    );
  }, [seriesData]);

  // RenderQuarterSummary
  const RenderQuarterSummary = useCallback(() => {
    return (
      <section className="mb-8 p-4 bg-white shadow rounded-lg">
        <h2 className="text-xl font-semibold mb-4">Quarter Summary</h2>
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
          {summaryData.length === 0 ? (
            <p className="text-gray-500">No quarter summary data available.</p>
          ) : (
            summaryData.map((quarterEntry, index) => (
              <div key={`${quarterEntry.quarter}-${quarterEntry.year}-${index}`} className="p-4 border rounded-lg shadow-sm bg-gray-50">
                <h3 className="text-lg font-medium mb-2">{quarterEntry.quarter} {quarterEntry.year}</h3>
                <p className="text-sm text-gray-600 mb-3">Total Epics: <span className="font-semibold">{quarterEntry.totalEpics}</span></p>
                <div className="space-y-2">
                  {quarterEntry.epicSummaries.map((statusSummary, statusIndex) => (
                    <div key={statusIndex} className="flex items-center justify-between text-sm">
                      <StatusBadge status={statusSummary.status} />
                      <span className="font-medium">{statusSummary.count} Epics</span>
                    </div>
                  ))}
                </div>
              </div>
            ))
          )}
        </div>
      </section>
    );
  }, [summaryData]);

  // RenderChangelogTable
  const RenderChangelogTable = useCallback(() => {
    const handleFilterChange = (filterName, value) => {
      const newFilters = { ...currentChangelogFilters, [filterName]: value === '' ? undefined : value };
      onApplyChangelogFilters(newFilters);
    };

    const handleClearFiltersClick = () => {
      onClearChangelogFilters();
    };

    return (
      <section className="p-4 bg-white shadow rounded-lg">
        <h2 className="text-xl font-semibold mb-4">Changelog</h2>

        {/* Filter Controls */}
        <div className="flex flex-wrap gap-4 mb-6 items-end">
          <div>
            <label htmlFor="entityTypeFilter" className="block text-sm font-medium text-gray-700">Entity Type</label>
            <select
              id="entityTypeFilter"
              className="mt-1 block w-full p-2 border border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm"
              value={currentChangelogFilters.entityType || ''}
              onChange={(e) => handleFilterChange('entityType', e.target.value)}
            >
              <option value="">All Types</option>
              {Object.values(ChangelogEntityType).map((type) => (
                <option key={type} value={type}>{type}</option>
              ))}
            </select>
          </div>

          <div>
            <label htmlFor="fieldFilter" className="block text-sm font-medium text-gray-700">Field</label>
            <input
              type="text"
              id="fieldFilter"
              className="mt-1 block w-full p-2 border border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm"
              value={currentChangelogFilters.field || ''}
              onChange={(e) => handleFilterChange('field', e.target.value)}
              placeholder="e.g., status, name"
            />
          </div>

          <div>
            <label htmlFor="startDateFilter" className="block text-sm font-medium text-gray-700">Start Date</label>
            <input
              type="date"
              id="startDateFilter"
              className="mt-1 block w-full p-2 border border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm"
              value={currentChangelogFilters.startDate || ''}
              onChange={(e) => handleFilterChange('startDate', e.target.value)}
            />
          </div>

          <div>
            <label htmlFor="endDateFilter" className="block text-sm font-medium text-gray-700">End Date</label>
            <input
              type="date"
              id="endDateFilter"
              className="mt-1 block w-full p-2 border border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm"
              value={currentChangelogFilters.endDate || ''}
              onChange={(e) => handleFilterChange('endDate', e.target.value)}
            />
          </div>

          <button
            onClick={handleClearFiltersClick}
            className="px-4 py-2 bg-gray-200 text-gray-800 rounded-md shadow-sm hover:bg-gray-300 focus:outline-none focus:ring-2 focus:ring-gray-500 focus:ring-offset-2"
          >
            Clear Filters
          </button>
        </div>

        {/* Changelog Table */}
        <div className="overflow-x-auto">
          <table className="min-w-full divide-y divide-gray-200">
            <thead className="bg-gray-50">
              <tr>
                <th scope="col" className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Timestamp</th>
                <th scope="col" className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Entity Type</th>
                <th scope="col" className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Entity Code</th>
                <th scope="col" className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Field</th>
                <th scope="col" className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Old Value</th>
                <th scope="col" className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">New Value</th>
              </tr>
            </thead>
            <tbody className="bg-white divide-y divide-gray-200">
              {changelogEntries.length === 0 ? (
                <tr>
                  <td colSpan="6" className="px-6 py-4 whitespace-nowrap text-sm text-gray-500 text-center">
                    No changelog entries found.
                  </td>
                </tr>
              ) : (
                changelogEntries.map((entry) => (
                  <tr key={entry.id}>
                    <td className="px-6 py-4 whitespace-nowrap text-sm text-gray-900">{entry.timestamp}</td>
                    <td className="px-6 py-4 whitespace-nowrap text-sm text-gray-900">{entry.entityType}</td>
                    <td className="px-6 py-4 whitespace-nowrap text-sm text-gray-900">{entry.entityCode}</td>
                    <td className="px-6 py-4 whitespace-nowrap text-sm text-gray-900">{entry.field}</td>
                    <td className="px-6 py-4 whitespace-nowrap text-sm text-gray-900">{entry.oldValue}</td>
                    <td className="px-6 py-4 whitespace-nowrap text-sm text-gray-900">{entry.newValue}</td>
                  </tr>
                ))
              )}
            </tbody>
          </table>
        </div>
      </section>
    );
  }, [changelogEntries, currentChangelogFilters, onApplyChangelogFilters, onClearChangelogFilters]);

  return (
    <div className="container mx-auto p-4">
      <h1 className="text-3xl font-bold mb-6">Reports Overview</h1>
      <RenderProgressTrendChart />
      <RenderQuarterSummary />
      <RenderChangelogTable />
    </div>
  );
}