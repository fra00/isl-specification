/**
 * DO NOT EDIT THIS FILE DIRECTLY
 * ----------------------------------
 * This file was generated by ISL Compiler.
 * Source: dungeon-game-over.isl.md
 * Edit the ISL file instead.
 */

import React, { useState, useEffect } from 'react';

export default function DungeonGameOver({ isOpen = false, onExit = () => {} }) {
  const [isAnimating, setIsAnimating] = useState(false);

  useEffect(() => {
    if (isOpen) {
      // Trigger the dramatic fade-in and scale animation shortly after mounting
      const animationTimer = setTimeout(() => {
        setIsAnimating(true);
      }, 50);
      return () => clearTimeout(animationTimer);
    } else {
      setIsAnimating(false);
    }
  }, [isOpen]);

  if (!isOpen) {
    return null;
  }

  return (
    <div className="fixed inset-0 z-[90] bg-black flex items-center justify-center p-4">
      <div
        className={`text-center transition-all duration-1000 ease-out transform ${
          isAnimating ? 'opacity-100 scale-100' : 'opacity-0 scale-50'
        }`}
      >
        <h1 className="text-6xl md:text-8xl text-red-700 font-serif font-bold mb-6 tracking-widest drop-shadow-[0_0_20px_rgba(185,28,28,0.8)]">
          GAME OVER
        </h1>
        
        <h2 className="text-2xl md:text-4xl text-gray-200 mb-4 font-serif">
          Tutti gli eroi sono caduti...
        </h2>
        
        <p className="text-lg md:text-xl text-gray-400 mb-12 italic">
          Zargon ha trionfato. Il mondo precipita nell'oscurità.
        </p>
        
        <button
          onClick={onExit}
          className="px-8 py-4 bg-red-950 hover:bg-red-800 text-red-100 font-bold text-lg rounded border border-red-800 transition-all duration-300 shadow-[0_0_15px_rgba(153,27,27,0.5)] hover:shadow-[0_0_30px_rgba(220,38,38,0.8)] uppercase tracking-wider cursor-pointer"
        >
          Torna al Menu
        </button>
      </div>
    </div>
  );
}