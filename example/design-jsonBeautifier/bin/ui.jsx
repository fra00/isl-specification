/**
 * 🛑 DO NOT EDIT THIS FILE DIRECTLY
 * ----------------------------------
 * This file was generated by ISL Compiler.
 * Source: ui.isl.md
 * Edit the ISL file instead.
 */

import React from 'react';

// Import from the real implementation context, using named exports as per signature
// The `beautifyJson` and `copyToClipboard` functions are available from the logic module.
// Although JsonOutputArea's ISL reference mentions JsonBeautifierService for copy functionality,
// the REAL IMPLEMENTATION CONTEXT provides `copyToClipboard` directly.
// For this specific UI component, `copyToClipboard` is not directly used here,
// but it's good practice to acknowledge its availability if it were needed.
// The `onCopy` prop in JsonOutputArea is an abstraction for the parent to handle the actual copy logic.
// Therefore, `beautifyJson` and `copyToClipboard` are not directly imported into this UI file
// as per the ISL specification for `JsonOutputArea`'s `triggerCopy` capability, which only calls `onCopy`.
// If the ISL had specified `JsonOutputArea` to directly perform the copy, then `copyToClipboard` would be imported here.
// For now, no direct import from "./logic" is needed in this UI file based on the current ISL flow.

// Component: JsonInputArea
// Role: Presentation
// Signature: { value: string, onChange: (newValue: string) => void }
export function JsonInputArea({ value, onChange }) {
  // Capability: updateValue
  // Contract: Updates the internal value of the input area and notifies the parent component of the change.
  // Flow:
  //   1. Extract the new string value from the `event`.
  //   2. Pass the new string value to the `onChange` callback.
  const updateValue = (event) => {
    const newValue = event.target.value;
    onChange(newValue);
  };

  return (
    <div className="flex flex-col space-y-2">
      <label htmlFor="raw-json-input" className="font-semibold text-gray-700">Raw JSON</label>
      <textarea
        id="raw-json-input"
        className="w-full h-48 p-3 border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 font-mono text-sm resize-y"
        value={value}
        onChange={updateValue}
        aria-label="Raw JSON input area"
      />
    </div>
  );
}

// Component: JsonOutputArea
// Role: Presentation
// Signature: { value: string, onCopy: () => void }
export function JsonOutputArea({ value, onCopy }) {
  // Capability: triggerCopy
  // Contract: Notifies the parent component that the copy action has been requested.
  // Flow:
  //   1. When the "Copy JSON" button is clicked, execute the `onCopy` callback.
  const triggerCopy = () => {
    onCopy();
  };

  return (
    <div className="flex flex-col space-y-2">
      <label htmlFor="beautified-json-output" className="font-semibold text-gray-700">Beautified JSON</label>
      <textarea
        id="beautified-json-output"
        className="w-full h-48 p-3 border border-gray-300 rounded-lg shadow-sm bg-gray-50 font-mono text-sm resize-y"
        value={value}
        readOnly
        aria-label="Beautified JSON output area"
      />
      <button
        type="button"
        onClick={triggerCopy}
        className="mt-2 px-4 py-2 bg-green-600 text-white font-medium rounded-lg shadow-md hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-green-500 focus:ring-offset-2 transition duration-150 ease-in-out self-end"
        aria-label="Copy beautified JSON to clipboard"
      >
        Copy JSON
      </button>
    </div>
  );
}

// Component: BeautifyButton
// Role: Presentation
// Signature: { onClick: () => void }
export function BeautifyButton({ onClick }) {
  // Capability: triggerBeautify
  // Contract: Notifies the parent component that the beautify action has been requested.
  // Flow:
  //   1. When the "Beautify JSON" button is clicked, execute the `onClick` callback.
  const triggerBeautify = () => {
    onClick();
  };

  return (
    <button
      type="button"
      onClick={triggerBeautify}
      className="px-6 py-3 bg-blue-600 text-white font-bold rounded-lg shadow-lg hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 transition duration-150 ease-in-out w-full sm:w-auto"
      aria-label="Beautify JSON"
    >
      Beautify JSON
    </button>
  );
}