/**
 * 🛑 DO NOT EDIT THIS FILE DIRECTLY
 * ----------------------------------
 * This file was generated by ISL Compiler.
 * Source: game-domain.isl.md
 * Edit the ISL file instead.
 */

// Enums
export const GameStatusEnum = {
  INITIAL: "INITIAL",
  PLAYING: "PLAYING",
  PAUSED: "PAUSED",
  GAME_OVER: "GAME_OVER",
  LEVEL_CLEARED: "LEVEL_CLEARED",
  GAME_WON: "GAME_WON",
};

export const EntityTypeEnum = {
  PLAYER: "PLAYER",
  INVADER: "INVADER",
  PLAYER_BULLET: "PLAYER_BULLET",
  INVADER_BULLET: "INVADER_BULLET",
  SHIELD_SEGMENT: "SHIELD_SEGMENT",
};

export const DirectionEnum = {
  LEFT: "LEFT",
  RIGHT: "RIGHT",
  UP: "UP",
  DOWN: "DOWN",
};

export const PlayerActionEnum = {
  MOVE_LEFT: "MOVE_LEFT",
  MOVE_RIGHT: "MOVE_RIGHT",
  SHOOT: "SHOOT",
};

export const InvaderTypeEnum = {
  TYPE_A: "TYPE_A",
  TYPE_B: "TYPE_B",
  TYPE_C: "TYPE_C",
  UFO: "UFO",
};

// Domain Entities (Factory Functions)
export const Position = (data = {}) => ({
  x: data.x != null ? data.x : 0,
  y: data.y != null ? data.y : 0,
});

export const Size = (data = {}) => ({
  width: data.width != null ? data.width : 0,
  height: data.height != null ? data.height : 0,
});

export const Velocity = (data = {}) => ({
  dx: data.dx != null ? data.dx : 0,
  dy: data.dy != null ? data.dy : 0,
});

export const PlayerState = (data = {}) => ({
  position: Position(data.position),
  size: Size(data.size),
  speed: data.speed != null ? data.speed : 0,
  lives: data.lives != null ? data.lives : 3,
  isShooting: data.isShooting != null ? data.isShooting : false,
  lastShotTime: data.lastShotTime != null ? data.lastShotTime : 0,
});

export const InvaderState = (data = {}) => ({
  id:
    data.id != null
      ? String(data.id)
      : `invader-${Math.random().toString(36).substring(2, 9)}`,
  position: Position(data.position),
  size: Size(data.size),
  type: data.type != null ? data.type : InvaderTypeEnum.TYPE_A,
  isAlive: data.isAlive != null ? data.isAlive : true,
  direction: data.direction != null ? data.direction : DirectionEnum.RIGHT,
  speed: data.speed != null ? data.speed : 0,
  points: data.points != null ? data.points : 0,
});

export const BulletState = (data = {}) => ({
  id:
    data.id != null
      ? String(data.id)
      : `bullet-${Math.random().toString(36).substring(2, 9)}`,
  position: Position(data.position),
  size: Size(data.size),
  speed: data.speed != null ? data.speed : 0,
  direction: data.direction != null ? data.direction : DirectionEnum.UP,
  owner: data.owner != null ? data.owner : EntityTypeEnum.PLAYER,
  isAlive: data.isAlive != null ? data.isAlive : true,
});

export const ShieldSegmentState = (data = {}) => ({
  id:
    data.id != null
      ? String(data.id)
      : `shield-${Math.random().toString(36).substring(2, 9)}`,
  position: Position(data.position),
  size: Size(data.size),
  health: data.health != null ? data.health : 1,
  isDestroyed: data.isDestroyed != null ? data.isDestroyed : false,
});

export const LevelConfig = (data = {}) => ({
  levelNumber: data.levelNumber != null ? data.levelNumber : 1,
  invaderRows: data.invaderRows != null ? data.invaderRows : 5,
  invaderCols: data.invaderCols != null ? data.invaderCols : 11,
  invaderSpeedMultiplier:
    data.invaderSpeedMultiplier != null ? data.invaderSpeedMultiplier : 1.0,
  invaderBulletSpeedMultiplier:
    data.invaderBulletSpeedMultiplier != null
      ? data.invaderBulletSpeedMultiplier
      : 1.0,
  invaderShotFrequencyMultiplier:
    data.invaderShotFrequencyMultiplier != null
      ? data.invaderShotFrequencyMultiplier
      : 1.0,
  playerBulletSpeedMultiplier:
    data.playerBulletSpeedMultiplier != null
      ? data.playerBulletSpeedMultiplier
      : 1.0,
  playerSpeedMultiplier:
    data.playerSpeedMultiplier != null ? data.playerSpeedMultiplier : 1.0,
  initialPlayerLives:
    data.initialPlayerLives != null ? data.initialPlayerLives : 3,
  shieldConfiguration: Array.isArray(data.shieldConfiguration)
    ? data.shieldConfiguration.map(Position)
    : [],
});

export const GameConfig = (data = {}) => ({
  canvasWidth: data.canvasWidth != null ? data.canvasWidth : 800,
  canvasHeight: data.canvasHeight != null ? data.canvasHeight : 600,
  playerBaseSpeed: data.playerBaseSpeed != null ? data.playerBaseSpeed : 200,
  playerBulletBaseSpeed:
    data.playerBulletBaseSpeed != null ? data.playerBulletBaseSpeed : 300,
  playerBulletCooldown:
    data.playerBulletCooldown != null ? data.playerBulletCooldown : 500,
  invaderBaseSpeed: data.invaderBaseSpeed != null ? data.invaderBaseSpeed : 20, //50,
  invaderBulletBaseSpeed:
    data.invaderBulletBaseSpeed != null ? data.invaderBulletBaseSpeed : 150,
  invaderShotBaseFrequency:
    data.invaderShotBaseFrequency != null
      ? data.invaderShotBaseFrequency
      : 2000,
  invaderHorizontalSpacing:
    data.invaderHorizontalSpacing != null ? data.invaderHorizontalSpacing : 40,
  invaderVerticalSpacing:
    data.invaderVerticalSpacing != null ? data.invaderVerticalSpacing : 30,
  invaderDescentAmount:
    data.invaderDescentAmount != null ? data.invaderDescentAmount : 10, //20,
  initialLives: data.initialLives != null ? data.initialLives : 3,
  maxLevels: data.maxLevels != null ? data.maxLevels : 10,
  shieldCount: data.shieldCount != null ? data.shieldCount : 4,
  shieldSegmentHealth:
    data.shieldSegmentHealth != null ? data.shieldSegmentHealth : 3,
  scorePerInvader: data.scorePerInvader != null ? data.scorePerInvader : 10,
  scorePerUFO: data.scorePerUFO != null ? data.scorePerUFO : 100,
  scorePerLevelClear:
    data.scorePerLevelClear != null ? data.scorePerLevelClear : 500,
});

export const GameConstants = (data = {}) => ({
  PLAYER_WIDTH: data.PLAYER_WIDTH != null ? data.PLAYER_WIDTH : 50,
  PLAYER_HEIGHT: data.PLAYER_HEIGHT != null ? data.PLAYER_HEIGHT : 20,
  INVADER_WIDTH: data.INVADER_WIDTH != null ? data.INVADER_WIDTH : 30,
  INVADER_HEIGHT: data.INVADER_HEIGHT != null ? data.INVADER_HEIGHT : 20,
  BULLET_WIDTH: data.BULLET_WIDTH != null ? data.BULLET_WIDTH : 4,
  BULLET_HEIGHT: data.BULLET_HEIGHT != null ? data.BULLET_HEIGHT : 10,
  SHIELD_SEGMENT_WIDTH:
    data.SHIELD_SEGMENT_WIDTH != null ? data.SHIELD_SEGMENT_WIDTH : 10,
  SHIELD_SEGMENT_HEIGHT:
    data.SHIELD_SEGMENT_HEIGHT != null ? data.SHIELD_SEGMENT_HEIGHT : 10,
  PLAYER_START_Y_OFFSET:
    data.PLAYER_START_Y_OFFSET != null ? data.PLAYER_START_Y_OFFSET : 50,
  INVADER_START_Y_OFFSET:
    data.INVADER_START_Y_OFFSET != null ? data.INVADER_START_Y_OFFSET : 50,
  UFO_SPAWN_INTERVAL:
    data.UFO_SPAWN_INTERVAL != null ? data.UFO_SPAWN_INTERVAL : 20000,
  UFO_SPEED: data.UFO_SPEED != null ? data.UFO_SPEED : 100,
  UFO_Y_POSITION: data.UFO_Y_POSITION != null ? data.UFO_Y_POSITION : 30,
  INVADER_MIN_X_OFFSET:
    data.INVADER_MIN_X_OFFSET != null ? data.INVADER_MIN_X_OFFSET : 20,
  INVADER_MAX_X_OFFSET:
    data.INVADER_MAX_X_OFFSET != null ? data.INVADER_MAX_X_OFFSET : 780,
});
