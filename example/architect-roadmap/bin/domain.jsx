/**
 * DO NOT EDIT THIS FILE DIRECTLY
 * ----------------------------------
 * This file was generated by ISL Compiler.
 * Source: domain.isl.md
 * Edit the ISL file instead.
 */

const generateId = () => crypto.randomUUID();
const getCurrentISODate = () => new Date().toISOString();

export const ProjectStatus = {
  PLANNING: 'PLANNING',
  ACTIVE: 'ACTIVE',
  COMPLETED: 'COMPLETED',
  PAUSED: 'PAUSED',
};

export const EpicStatus = {
  NOT_STARTED: 'NOT_STARTED',
  IN_PROGRESS: 'IN_PROGRESS',
  COMPLETED: 'COMPLETED',
  BLOCKED: 'BLOCKED',
};

export const StoryStatus = {
  NOT_STARTED: 'NOT_STARTED',
  IN_PROGRESS: 'IN_PROGRESS',
  COMPLETED: 'COMPLETED',
  BLOCKED: 'BLOCKED',
};

export const Priority = {
  LOW: 'LOW',
  MEDIUM: 'MEDIUM',
  HIGH: 'HIGH',
  CRITICAL: 'CRITICAL',
};

export const RiskLevel = {
  LOW: 'LOW',
  MEDIUM: 'MEDIUM',
  HIGH: 'HIGH',
};

export const StoryPoints = {
  ONE: 1,
  TWO: 2,
  THREE: 3,
  FIVE: 5,
  EIGHT: 8,
  THIRTEEN: 13,
  TWENTY_ONE: 21,
};

export const ChangelogEntityType = {
  PROJECT: 'PROJECT',
  EPIC: 'EPIC',
  STORY: 'STORY',
  TEAM_MEMBER: 'TEAM_MEMBER',
  CATEGORY: 'CATEGORY',
};

export const ImportMode = {
  REPLACE_ALL: 'REPLACE_ALL',
  ADD_TO_EXISTING: 'ADD_TO_EXISTING',
};

export const ProjectEntity = (data = {}) => ({
  id: data.id || generateId(),
  name: data.name || '',
  description: data.description || '',
  owner: data.owner || '',
  plannedStartDate: data.plannedStartDate || '',
  plannedEndDate: data.plannedEndDate || '',
  status: data.status || ProjectStatus.PLANNING,
  progress: data.progress ?? 0,
  createdAt: data.createdAt || getCurrentISODate(),
  updatedAt: data.updatedAt || getCurrentISODate(),
});

export const EpicEntity = (data = {}) => ({
  id: data.id || generateId(),
  projectId: data.projectId || '',
  code: data.code || '',
  title: data.title || '',
  description: data.description || '',
  priority: data.priority || Priority.MEDIUM,
  quarter: data.quarter || '',
  year: data.year ?? new Date().getFullYear(),
  plannedStartDate: data.plannedStartDate || '',
  plannedEndDate: data.plannedEndDate || '',
  category: data.category || '',
  owner: data.owner || '',
  riskLevel: data.riskLevel || RiskLevel.MEDIUM,
  jiraRef: data.jiraRef || '',
  isBlocked: data.isBlocked ?? false,
  createdAt: data.createdAt || getCurrentISODate(),
  updatedAt: data.updatedAt || getCurrentISODate(),
});

export const StoryEntity = (data = {}) => ({
  id: data.id || generateId(),
  epicId: data.epicId || '',
  code: data.code || '',
  title: data.title || '',
  description: data.description || '',
  priority: data.priority || Priority.MEDIUM,
  storyPoints: data.storyPoints || StoryPoints.ONE,
  assignee: data.assignee || '',
  sprint: data.sprint || '',
  plannedStartDate: data.plannedStartDate || '',
  plannedEndDate: data.plannedEndDate || '',
  jiraRef: data.jiraRef || '',
  notes: data.notes || '',
  externalLinks: data.externalLinks || [],
  progress: data.progress ?? 0,
  isBlocked: data.isBlocked ?? false,
  createdAt: data.createdAt || getCurrentISODate(),
  updatedAt: data.updatedAt || getCurrentISODate(),
});

export const ChangelogEntry = (data = {}) => ({
  id: data.id || generateId(),
  entityType: data.entityType || ChangelogEntityType.PROJECT,
  entityId: data.entityId || '',
  entityCode: data.entityCode || '',
  field: data.field || '',
  oldValue: data.oldValue || '',
  newValue: data.newValue || '',
  timestamp: data.timestamp || getCurrentISODate(),
});

export const TeamMember = (data = {}) => ({
  id: data.id || generateId(),
  name: data.name || '',
  createdAt: data.createdAt || getCurrentISODate(),
  updatedAt: data.updatedAt || getCurrentISODate(),
});

export const Category = (data = {}) => ({
  id: data.id || generateId(),
  name: data.name || '',
  createdAt: data.createdAt || getCurrentISODate(),
  updatedAt: data.updatedAt || getCurrentISODate(),
});

export const ExportedData = (data = {}) => ({
  version: data.version || '1.0.0',
  timestamp: data.timestamp || getCurrentISODate(),
  projects: data.projects || [],
  epics: data.epics || [],
  stories: data.stories || [],
  changelog: data.changelog || [],
  teamMembers: data.teamMembers || [],
  categories: data.categories || [],
});