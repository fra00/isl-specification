/**
 * 🛑 DO NOT EDIT THIS FILE DIRECTLY
 * ----------------------------------
 * This file was generated by ISL Compiler.
 * Source: hud-presentation.isl.md
 * Edit the ISL file instead.
 */

import React from 'react';

/**
 * HudPresentation component
 * Renders the Head-Up Display elements (Score, Lives, Level) based on the game state.
 * @param {object} props - The component props.
 * @param {object} props.gameState - The current game state.
 */
const HudPresentation = ({ gameState }) => {
  // Retrieve score from gameState, with null-safe access and default value.
  const score = gameState?.score != null ? gameState.score : 0;
  // Retrieve playerState.lives from gameState, with null-safe access and default value.
  const lives = gameState?.playerState?.lives != null ? gameState.playerState.lives : 0;
  // Retrieve levelNumber from gameState, with null-safe access and default value.
  const levelNumber = gameState?.levelNumber != null ? gameState.levelNumber : 1;

  return (
    <div className="absolute top-0 left-0 right-0 p-4 text-white text-2xl font-bold flex justify-between items-center">
      <div className="flex-1 text-left">
        Score: {score}
      </div>
      <div className="flex-1 text-center">
        Level: {levelNumber}
      </div>
      <div className="flex-1 text-right">
        Lives: {lives}
      </div>
    </div>
  );
};

export default HudPresentation;