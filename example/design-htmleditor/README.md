# create

```
npx ts-node tools/vscode-isl/src/isl-create.ts example/htmleditor "Genera un editor WYSWYG due colonne a sx un text editor che permette di inserire html a dx un render dell'html scritto" --gemini
```

# build

```
npx ts-node tools/vscode-isl/src/isl-builder.ts example/htmleditor
```

# generate

```
npx ts-node tools/vscode-isl/src/isl-generator.ts example/htmleditor --gemini
```
