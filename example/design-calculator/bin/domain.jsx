/**
 * 🛑 DO NOT EDIT THIS FILE DIRECTLY
 * ----------------------------------
 * This file was generated by ISL Compiler.
 * Source: domain.isl.md
 * Edit the ISL file instead.
 */

/**
 * Defines the types of arithmetic operations supported by the calculator.
 * @type {Readonly<{ADD: string, SUBTRACT: string, MULTIPLY: string, DIVIDE: string}>}
 */
export const OperationType = Object.freeze({
  ADD: 'ADD',
  SUBTRACT: 'SUBTRACT',
  MULTIPLY: 'MULTIPLY',
  DIVIDE: 'DIVIDE',
});

/**
 * Factory function to create a CalculatorStateEntity object.
 * Represents the complete internal state of the calculator at any given moment.
 *
 * @param {object} [data={}] - Optional initial data for the calculator state.
 * @param {string} [data.currentOperand=''] - The number currently being entered or the result of the last operation.
 * @param {string} [data.previousOperand=''] - The first operand in a pending operation.
 * @param {OperationType | null} [data.operator=null] - The arithmetic operation selected, or null.
 * @param {string} [data.displayValue='0'] - The value currently shown on the calculator's display.
 * @param {boolean} [data.waitingForNewOperand=false] - Indicates if the next digit input should clear currentOperand.
 * @param {boolean} [data.isError=false] - Indicates if the calculator is in an error state.
 * @returns {{currentOperand: string, previousOperand: string, operator: OperationType | null, displayValue: string, waitingForNewOperand: boolean, isError: boolean}}
 */
export const CalculatorStateEntity = (data = {}) => {
  const {
    currentOperand = '',
    previousOperand = '',
    operator = null,
    displayValue = '0', // Default display value for a new calculator state
    waitingForNewOperand = false,
    isError = false,
  } = data;

  return {
    currentOperand,
    previousOperand,
    operator,
    displayValue,
    waitingForNewOperand,
    isError,
  };
};