/**
 * DO NOT EDIT THIS FILE DIRECTLY
 * ----------------------------------
 * This file was generated by ISL Compiler.
 * Source: settings-presentation.isl.md
 * Edit the ISL file instead.
 */

import React, { useState, useEffect, useCallback, useRef } from 'react';
import { TeamMember, Category } from './domain'; // Used for type inference in state and logic
import { SettingsLogic } from './settings-logic'; // Used for type inference of the prop
import { ConfirmationDialog, useNotification } from './ui-components';

// Helper to ensure input is not empty
const isNotEmpty = (str) => str.trim() !== '';

export default function SettingsPresentation({ settingsLogic }) {
  const [teamMembers, setTeamMembers] = useState([]);
  const [categories, setCategories] = useState([]);

  const [newTeamMemberName, setNewTeamMemberName] = useState('');
  const [editingTeamMemberId, setEditingTeamMemberId] = useState(null);
  const [editingTeamMemberName, setEditingTeamMemberName] = useState('');

  const [newCategoryName, setNewCategoryName] = useState('');
  const [editingCategoryId, setEditingCategoryId] = useState(null);
  const [editingCategoryName, setEditingCategoryName] = useState('');

  const [isConfirmDialogOpen, setIsConfirmDialogOpen] = useState(false);
  const [confirmDialogMessage, setConfirmDialogMessage] = useState('');
  const confirmActionRef = useRef(() => {}); // Use ref for stable callback
  const confirmCancelActionRef = useRef(() => {}); // Use ref for stable callback

  const [isResetConfirmDialogOpen, setIsResetConfirmDialogOpen] = useState(false);

  const { showSuccess, showError } = useNotification();

  const loadTeamMembers = useCallback(async () => {
    try {
      const members = await settingsLogic.getTeamMembers();
      setTeamMembers(members);
    } catch (error) {
      showError('Failed to load team members: ' + (error.message || 'An unknown error occurred.'));
    }
  }, [settingsLogic, showError]);

  const loadCategories = useCallback(async () => {
    try {
      const cats = await settingsLogic.getCategories();
      setCategories(cats);
    } catch (error) {
      showError('Failed to load categories: ' + (error.message || 'An unknown error occurred.'));
    }
  }, [settingsLogic, showError]);

  useEffect(() => {
    loadTeamMembers();
    loadCategories();
  }, [loadTeamMembers, loadCategories]);

  const handleAddTeamMember = useCallback(async () => {
    if (!isNotEmpty(newTeamMemberName)) {
      showError('Team member name cannot be empty.');
      return;
    }
    try {
      await settingsLogic.addTeamMember(newTeamMemberName);
      showSuccess('Team member added successfully.');
      setNewTeamMemberName('');
      loadTeamMembers();
    } catch (error) {
      showError('Failed to add team member: ' + (error.message || 'An unknown error occurred.'));
    }
  }, [newTeamMemberName, settingsLogic, showSuccess, showError, loadTeamMembers]);

  const startEditTeamMember = useCallback((member) => {
    setEditingTeamMemberId(member.id);
    setEditingTeamMemberName(member.name);
  }, []);

  const cancelEditTeamMember = useCallback(() => {
    setEditingTeamMemberId(null);
    setEditingTeamMemberName('');
  }, []);

  const handleEditTeamMember = useCallback(async (id) => {
    if (!isNotEmpty(editingTeamMemberName)) {
      showError('Team member name cannot be empty.');
      return;
    }
    try {
      await settingsLogic.updateTeamMember(id, editingTeamMemberName);
      showSuccess('Team member updated successfully.');
      cancelEditTeamMember();
      loadTeamMembers();
    } catch (error) {
      showError('Failed to update team member: ' + (error.message || 'An unknown error occurred.'));
    }
  }, [editingTeamMemberName, settingsLogic, showSuccess, showError, cancelEditTeamMember, loadTeamMembers]);

  const handleDeleteTeamMember = useCallback((id) => {
    setConfirmDialogMessage('Are you sure you want to delete this team member? This action cannot be undone.');
    confirmActionRef.current = async () => {
      try {
        await settingsLogic.deleteTeamMember(id);
        showSuccess('Team member deleted successfully.');
        loadTeamMembers();
      } catch (error) {
        showError('Failed to delete team member: ' + (error.message || 'An unknown error occurred.'));
      } finally {
        setIsConfirmDialogOpen(false);
      }
    };
    confirmCancelActionRef.current = () => {
      setIsConfirmDialogOpen(false);
    };
    setIsConfirmDialogOpen(true);
  }, [settingsLogic, showSuccess, showError, loadTeamMembers]);

  const handleAddCategory = useCallback(async () => {
    if (!isNotEmpty(newCategoryName)) {
      showError('Category name cannot be empty.');
      return;
    }
    try {
      await settingsLogic.addCategory(newCategoryName);
      showSuccess('Category added successfully.');
      setNewCategoryName('');
      loadCategories();
    } catch (error) {
      showError('Failed to add category: ' + (error.message || 'An unknown error occurred.'));
    }
  }, [newCategoryName, settingsLogic, showSuccess, showError, loadCategories]);

  const startEditCategory = useCallback((category) => {
    setEditingCategoryId(category.id);
    setEditingCategoryName(category.name);
  }, []);

  const cancelEditCategory = useCallback(() => {
    setEditingCategoryId(null);
    setEditingCategoryName('');
  }, []);

  const handleEditCategory = useCallback(async (id) => {
    if (!isNotEmpty(editingCategoryName)) {
      showError('Category name cannot be empty.');
      return;
    }
    try {
      await settingsLogic.updateCategory(id, editingCategoryName);
      showSuccess('Category updated successfully.');
      cancelEditCategory();
      loadCategories();
    } catch (error) {
      showError('Failed to update category: ' + (error.message || 'An unknown error occurred.'));
    }
  }, [editingCategoryName, settingsLogic, showSuccess, showError, cancelEditCategory, loadCategories]);

  const handleDeleteCategory = useCallback((id) => {
    setConfirmDialogMessage('Are you sure you want to delete this category? This action cannot be undone.');
    confirmActionRef.current = async () => {
      try {
        await settingsLogic.deleteCategory(id);
        showSuccess('Category deleted successfully.');
        loadCategories();
      } catch (error) {
        showError('Failed to delete category: ' + (error.message || 'An unknown error occurred.'));
      } finally {
        setIsConfirmDialogOpen(false);
      }
    };
    confirmCancelActionRef.current = () => {
      setIsConfirmDialogOpen(false);
    };
    setIsConfirmDialogOpen(true);
  }, [settingsLogic, showSuccess, showError, loadCategories]);

  const handleResetDatabase = useCallback(() => {
    setConfirmDialogMessage('WARNING: This will permanently delete ALL application data. Are you absolutely sure you want to proceed?');
    confirmActionRef.current = () => {
      setIsConfirmDialogOpen(false); // Close first dialog
      setIsResetConfirmDialogOpen(true); // Open second dialog
    };
    confirmCancelActionRef.current = () => {
      setIsConfirmDialogOpen(false);
    };
    setIsConfirmDialogOpen(true);
  }, []);

  const executeResetDatabase = useCallback(async () => {
    try {
      await settingsLogic.resetDatabase();
      showSuccess('Database reset successfully. The application will now reload.');
      // Delay reload slightly to allow notification to be seen
      setTimeout(() => window.location.reload(), 1500);
    } catch (error) {
      showError('Failed to reset database: ' + (error.message || 'An unknown error occurred.'));
    } finally {
      setIsResetConfirmDialogOpen(false);
    }
  }, [settingsLogic, showSuccess, showError]);

  const cancelResetDatabase = useCallback(() => {
    setIsResetConfirmDialogOpen(false);
  }, []);

  return (
    <div className="p-6 space-y-8">
      {/* Team Members Section */}
      <section className="bg-white p-6 rounded-lg shadow">
        <h2 className="text-2xl font-semibold mb-4">Manage Team Members</h2>
        <div className="flex space-x-2 mb-4">
          <input
            type="text"
            className="flex-grow p-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
            placeholder="New team member name"
            value={newTeamMemberName}
            onChange={(e) => setNewTeamMemberName(e.target.value)}
            onKeyDown={(e) => { if (e.key === 'Enter') handleAddTeamMember(); }}
          />
          <button
            className="px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 disabled:opacity-50 disabled:cursor-not-allowed"
            onClick={handleAddTeamMember}
            disabled={!isNotEmpty(newTeamMemberName)}
          >
            Add Team Member
          </button>
        </div>

        <ul className="divide-y divide-gray-200">
          {teamMembers.length === 0 ? (
            <li className="py-3 text-gray-500">No team members added yet.</li>
          ) : (
            teamMembers.map((member) => (
              <li key={member.id} className="py-3 flex items-center justify-between">
                {editingTeamMemberId === member.id ? (
                  <div className="flex-grow flex items-center space-x-2">
                    <input
                      type="text"
                      className="flex-grow p-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
                      value={editingTeamMemberName}
                      onChange={(e) => setEditingTeamMemberName(e.target.value)}
                      onKeyDown={(e) => { if (e.key === 'Enter') handleEditTeamMember(member.id); }}
                    />
                    <button
                      className="px-3 py-1 bg-green-600 text-white rounded-md hover:bg-green-700 text-sm disabled:opacity-50 disabled:cursor-not-allowed"
                      onClick={() => handleEditTeamMember(member.id)}
                      disabled={!isNotEmpty(editingTeamMemberName)}
                    >
                      Save
                    </button>
                    <button
                      className="px-3 py-1 bg-gray-400 text-white rounded-md hover:bg-gray-500 text-sm"
                      onClick={cancelEditTeamMember}
                    >
                      Cancel
                    </button>
                  </div>
                ) : (
                  <>
                    <span className="text-lg">{member.name}</span>
                    <div className="space-x-2">
                      <button
                        className="px-3 py-1 bg-yellow-500 text-white rounded-md hover:bg-yellow-600 text-sm"
                        onClick={() => startEditTeamMember(member)}
                      >
                        Edit
                      </button>
                      <button
                        className="px-3 py-1 bg-red-600 text-white rounded-md hover:bg-red-700 text-sm"
                        onClick={() => handleDeleteTeamMember(member.id)}
                      >
                        Delete
                      </button>
                    </div>
                  </>
                )}
              </li>
            ))
          )}
        </ul>
      </section>

      {/* Categories Section */}
      <section className="bg-white p-6 rounded-lg shadow">
        <h2 className="text-2xl font-semibold mb-4">Manage Categories</h2>
        <div className="flex space-x-2 mb-4">
          <input
            type="text"
            className="flex-grow p-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
            placeholder="New category name"
            value={newCategoryName}
            onChange={(e) => setNewCategoryName(e.target.value)}
            onKeyDown={(e) => { if (e.key === 'Enter') handleAddCategory(); }}
          />
          <button
            className="px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 disabled:opacity-50 disabled:cursor-not-allowed"
            onClick={handleAddCategory}
            disabled={!isNotEmpty(newCategoryName)}
          >
            Add Category
          </button>
        </div>

        <ul className="divide-y divide-gray-200">
          {categories.length === 0 ? (
            <li className="py-3 text-gray-500">No categories added yet.</li>
          ) : (
            categories.map((category) => (
              <li key={category.id} className="py-3 flex items-center justify-between">
                {editingCategoryId === category.id ? (
                  <div className="flex-grow flex items-center space-x-2">
                    <input
                      type="text"
                      className="flex-grow p-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
                      value={editingCategoryName}
                      onChange={(e) => setEditingCategoryName(e.target.value)}
                      onKeyDown={(e) => { if (e.key === 'Enter') handleEditCategory(category.id); }}
                    />
                    <button
                      className="px-3 py-1 bg-green-600 text-white rounded-md hover:bg-green-700 text-sm disabled:opacity-50 disabled:cursor-not-allowed"
                      onClick={() => handleEditCategory(category.id)}
                      disabled={!isNotEmpty(editingCategoryName)}
                    >
                      Save
                    </button>
                    <button
                      className="px-3 py-1 bg-gray-400 text-white rounded-md hover:bg-gray-500 text-sm"
                      onClick={cancelEditCategory}
                    >
                      Cancel
                    </button>
                  </div>
                ) : (
                  <>
                    <span className="text-lg">{category.name}</span>
                    <div className="space-x-2">
                      <button
                        className="px-3 py-1 bg-yellow-500 text-white rounded-md hover:bg-yellow-600 text-sm"
                        onClick={() => startEditCategory(category)}
                      >
                        Edit
                      </button>
                      <button
                        className="px-3 py-1 bg-red-600 text-white rounded-md hover:bg-red-700 text-sm"
                        onClick={() => handleDeleteCategory(category.id)}
                      >
                        Delete
                      </button>
                    </div>
                  </>
                )}
              </li>
            ))
          )}
        </ul>
      </section>

      {/* Application Options Section */}
      <section className="bg-white p-6 rounded-lg shadow">
        <h2 className="text-2xl font-semibold mb-4">Application Options</h2>
        <p className="text-gray-700 mb-4">
          This section contains critical application settings. Use with caution.
        </p>
        <button
          className="px-6 py-3 bg-red-700 text-white rounded-md hover:bg-red-800 focus:outline-none focus:ring-2 focus:ring-red-700 focus:ring-offset-2"
          onClick={handleResetDatabase}
        >
          Reset Database
        </button>
        <p className="text-sm text-gray-500 mt-2">
          WARNING: This action is irreversible and will delete all your data.
        </p>
        {/* Informational text regarding application version and IndexedDB storage usage could be added here */}
      </section>

      <ConfirmationDialog
        isOpen={isConfirmDialogOpen}
        message={confirmDialogMessage}
        onConfirm={confirmActionRef.current}
        onCancel={confirmCancelActionRef.current}
      />

      <ConfirmationDialog
        isOpen={isResetConfirmDialogOpen}
        message="Are you absolutely sure you want to proceed with the database reset? This action cannot be undone."
        onConfirm={executeResetDatabase}
        onCancel={cancelResetDatabase}
      />
    </div>
  );
}